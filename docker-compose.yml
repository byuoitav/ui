version: "3"
services:
  ui:
    image: docker.pkg.github.com/byuoitav/ui/amd64:v0.1.1
    ports:
      - "80:8080"
    environment:
      - SYSTEM_ID=$HOST
      - DB_ADDRESS=$DB_ADDRESS
      - DB_USERNAME=$DB_USERNAME
      - DB_PASSWORD=$DB_PASSWORD
    network_mode: "host"
    restart: always
    tty: true
